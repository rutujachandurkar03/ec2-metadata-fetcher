apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metadata-fetcher.fullname" . }}-test-connection"
  labels:
    {{- include "metadata-fetcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metadata-fetcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
